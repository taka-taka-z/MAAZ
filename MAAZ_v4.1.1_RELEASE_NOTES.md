# MAAZ v4.1.1 リリースノート

**リリース日:** 2026-04-05
**前バージョン:** v4.1.0

---

## 変更内容

### 1. スナップショット保存 — 不具合修正・機能拡充

**問題:** スナップショット保存ボタンが localStorage にのみデータを保存し、ユーザーへのフィードバックがなかったため、保存が機能していないように見えていた。

**修正内容:**
- 保存成功時にトースト通知を表示（「スナップショットを保存しました」）
- **スナップショットDLボタン**を新設 — ブラウザダウンロードで完全な JSON ファイルをエクスポート可能に
- スナップショットデータに `statuses`、`rtFlags`、`nistScores` を追加（従来は `ztCur`/`ztTgt` のみ）、完全な状態復元を実現
- スナップショット保持数を 5 → 10 に拡大

### 2. エグゼクティブレポート — コンテンツ拡充

**問題:** エグゼクティブレポートにはピラー別の簡易バーチャートと Critical Top 10 アクションのみが表示されており、ステークホルダー向けの成熟度情報やアクション計画の詳細が不足していた。

**追加内容:**
- **ピラー成熟度マップ（テーブル形式）:** 各 ZTMM ピラーの AS-IS / TO-BE / Gap / 脅威数（AS-IS）/ 脅威数（TO-BE）/ 新規緩和数を構造化テーブルで表示
- **フェーズ別アクション計画:** 実装フェーズごとに全アクションをグループ化して表示
  - Quick Win（Lv1-2）
  - Short Term（Lv3）
  - Strategic（Lv4）
- 各アクションに優先度バッジ（C/H/M）、ZTMM ID、機能名、レベル遷移、ピラー、脅威数、関連 NIST RMF 数を表示

### 3. ライトモード — 文字視認性修正

**問題:** 4箇所のインラインスタイルにダーク背景（`rgba(30,20,14,0.5-0.6)`）がハードコードされており、ライトモードで暗いパッチが残り文字が読めなくなっていた。

**修正内容:** 全4箇所を `var(--bg-secondary)` に置換。ライトモードでは `#ede4d8`、ダークモードでは `#180e08` に自動切替。

対象要素:
- 攻撃手法詳細パネル
- RT 検証フォーム
- サブテクニック項目
- NIST AI RMF スコープ外項目

---

## 互換性

- JSON エクスポート形式を `maaz-v4.1.1` に更新
- スナップショットエクスポート形式を `maaz-snapshot-v4.1.1` に更新
- インポートは `maaz-v4`、`maaz-v4.1.0`、`maaz-v4.1.1` の全形式に後方互換

---

## ファイル

- `MAAZ_v4.1.1.html` — 単一ファイル、依存パッケージ不要のアセスメントツール
